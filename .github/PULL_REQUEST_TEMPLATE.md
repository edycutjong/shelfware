## What this changes

## Why

## Checklist
- [ ] `make ci` passes (lint, typecheck, coverage-gated tests, api tests, audit, check)
- [ ] The judged path still runs live (`make demo`) — no mock introduced
- [ ] Any new number in README, DEMO or JUDGE is recounted from `data/census.json` or a committed receipt
- [ ] The word for a shelf wrapper is still CoinMarketCap's listing state, `untracked`

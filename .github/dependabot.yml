version: 2
updates:
  # The product has zero runtime dependencies; this covers requirements-dev.txt (pytest, ruff,
  # mypy, hypothesis, pip-audit). Security alerts are a repo setting and stay on regardless.
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
    groups:
      minor-and-patch:
        update-types: ["minor", "patch"]
    ignore:
      # Major bumps are migrations, not maintenance. They break CI on a frozen submission and
      # must be a deliberate, separate piece of work.
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    open-pull-requests-limit: 3
    groups:
      actions:
        patterns: ["*"]
